# refbias 라벨링 코드북 v2
2026-09-08 · 대상 `data/goldset_sample.csv` 2,867건

## 판정 단위
기사 1건. 제공 정보는 **제목 + 본문 앞 200자 + 언론사 + 일자**뿐이다.
본문 200자를 넘는 내용은 판정 근거로 쓸 수 없다(빅카인즈 저작권 정책).

## 1차 라벨 — 5범주 (택1)

| 코드 | 정의 | 판정 신호 |
|---|---|---|
| `EVENT` | **새로 발생한 사건**이 기사의 주된 대상 | 사건 발생 사실 서술, 일시·장소·피해 명시 |
| `REF` | **과거 사건**이 주된 대상. 이 보도 시점에 새 사건 없음 | 회고·추모·재조명·판결·후유증, "N년 전", "당시" |
| `INST` | **제도 대응**이 주된 대상 | 훈련·점검·대책·법령·예산·조직·협약·기술개발사업 |
| `OTHER` | 질의 주제와 관련은 있으나 사건도 제도도 아님 | 해설·기술·시장·산업동향·국제정세 배경·인물 |
| `IRRELEVANT` | **질의 주제와 무관.** 질의어가 우연히 포함됨 | 질의어가 본문에 흩어져 있을 뿐 주제가 다름 |

### 판정 순서 (위에서부터, 먼저 걸리는 것으로 확정)
1. 기사의 **주제**가 질의 대상(예: 화학사고, 테러, 핵실험)과 무관한가 → `IRRELEVANT`
2. **새로 발생한 사건**이 주된 대상인가 → `EVENT`
3. **과거 사건**이 주된 대상인가 → `REF`
4. **제도 대응**이 주된 대상인가 → `INST`
5. 나머지 → `OTHER`

### 핵심 원칙 — 「주된 대상」으로 판정한다
과거 사건 언급이 **삽입구로만** 등장하면 `REF`가 아니다.
> 「'구미 악몽' 떠올린 SK하이닉스 불소 사고」 → 주된 대상은 SK하이닉스 신규 사고 → `EVENT`
> (구미 언급은 앵커 재인용률 지표가 별도로 잡는다. 라벨과 혼동하지 말 것)

## 2차 — EVENT일 때만 사건 서술자
`NEW`/`FOLLOW` 구분은 기사 단독으로 판정할 수 없다. 라벨러는 사건을 **식별**만 하고,
클러스터링 스크립트가 동일 사건 내 최초 보도를 `NEW`, 나머지를 `FOLLOW`로 자동 분해한다.

| 필드 | 내용 | 예 |
|---|---|---|
| `ev_date` | 사건 발생일 추정 (YYYYMMDD, 불명 시 기사일자) | 20250809 |
| `ev_place` | 최소 행정단위 | 정읍시 북면 |
| `ev_object` | 대상·물질·수단 | 황산 |

세 필드가 일치하는 기사를 동일 사건으로 묶는다. 최초 일자 = `NEW`, 이후 = `FOLLOW`.

## 성분 항등식
```
V = NEW + FOLLOW + REF + INST + OTHER + IRRELEVANT
증폭계수      A = V / NEW
참조 성분     R = (REF + FOLLOW) / V
제도 성분     I = INST / V
질의 정밀도   P = (V - IRRELEVANT) / V
```
기존 연구의 「키워드 규칙 정밀도 0.431」은 `IRRELEVANT` 범주 없이는 재현·검증할 수 없다.
v1에서 이 범주를 신설한 이유다.

## 경계 사례 판례집
| 사례 | 라벨 | 근거 |
|---|---|---|
| 정읍 공장 황산 6천L 중 40L 유출, 인명피해 없음 (KBS) | `EVENT` | 신규 사건 발생 보도 |
| 정읍 제3산단 잇단 황산 유출, 8개월새 세 번째 (경향) | `EVENT` | 신규 사건이 주. 과거 2건은 맥락 |
| 4년 전 LG 인도공장 유독가스, 4년 뒤 가보니 (MBC) | `REF` | 과거 사건의 후속 취재. 신규 사건 없음 |
| 충북보건환경연구원, 축제장 고위험병원체 선제검사 | `INST` | 제도적 점검 활동 |
| 행복청 을지연습 다중이용시설 화재 대응 훈련 | `INST` | 훈련 |
| 국산 탄저균 백신 개발 활기 | `OTHER` | 산업·기술 동향 |
| 유해화학물질 누출 인과관계 대법원 판결 | `REF` | 과거 사고에 대한 사법 판단 |
| 국민의힘 의원 징계 기사 (질의 「테러 예고 글」에서 적중) | `IRRELEVANT` | 주제 무관 |
| 대만 방송사 미사일 공격 오보 (질의 「폭발물 테러」) | `IRRELEVANT` | 주제 무관 |
| 화학사고 오염수 이동형 처리 시스템 상용화 | `OTHER` | 기업 기술 상용화 |

## 라벨러 구성
| 라벨러 | 정체 | 역할 |
|---|---|---|
| A | LLM #1 | 전량 독립 라벨링 |
| B | LLM #2 (별도 모델) | 전량 독립 라벨링 |
| C | 인간 2인 | 무작위 400건 독립 라벨링 — 타당성 검증 |
| — | 빅카인즈 `사건/사고 분류` | 라벨링과 독립인 제3자 신호. 수렴 타당도 |

A·B 불일치 건은 조정(adjudication) 대상으로 분리해 보고한다.
κ는 A-B, A-C, B-C 각각 산출한다.

## 절차
1. **파일럿 100건** — A·B 독립 라벨링 → κ 산출 → 코드북 v2 조정
2. **본 라벨링 2,867건** — A·B 독립
3. **인간 400건** — 무작위 부분집합
4. 사건 클러스터링 → NEW/FOLLOW 분해
5. 성분 비율 산출 + Bootstrap 10,000 CI

---

# v2 개정 (2026-09-08, 파일럿 100건 결과 반영)

## 파일럿 결과
라벨러 A·B 독립 100건. **Cohen κ = 0.772** (Po 0.830 / Pe 0.256) — Landis-Koch 「상당(substantial)」.

| 범주 | A | B | 합치 | 진단 |
|---|---|---|---|---|
| EVENT | 33 | 32 | 32 | **거의 완전 일치.** 사건 판정은 견고 |
| REF | 3 | 1 | 1 | n 부족으로 판단 보류 |
| INST | 30 | 24 | 23 | 경계 일부 흔들림 |
| OTHER | 14 | 14 | 8 | **가장 불안정** |
| IRRELEVANT | 20 | 29 | 19 | B가 과잉 적용 |

불일치 17건의 구조: `OTHER→IRRELEVANT` 5 · `INST→OTHER` 4 · `INST→IRRELEVANT` 3.
**불일치가 전부 비사건 범주 사이에서만 발생했고, EVENT 경계는 1건뿐이다.**

이것이 뜻하는 바: 증폭계수 **A = V / NEW** 는 이미 신뢰할 수 있다. EVENT 판정이 흔들리지 않기 때문이다.
불확실성은 R(참조 성분)·I(제도 성분) 분해에만 남는다. v2는 그 경계를 정리한다.

## v2-1 IRRELEVANT 판정 규칙 (신설)
**필요성 기준**: 질의 대상 현상(화학사고·테러·핵실험 등)이 **기사 내용의 성립에 필요한가**를 묻는다.

- 필요하다 → 최소한 `OTHER` 이상. IRRELEVANT 아님
- 단어만 스쳐 지나가고 빼도 기사가 성립한다 → `IRRELEVANT`

| 사례 | 판정 | 근거 |
|---|---|---|
| 옴진리교 사린 사건이 원인으로 언급된 일본 종교 불신 조사 | `OTHER` | 사린 사건을 빼면 기사가 성립하지 않는다 |
| 「테러 예고 글」 질의에 걸린 국민의힘 의원 징계 기사 | `IRRELEVANT` | 질의어가 스쳐 지나갈 뿐 |
| 「불산 누출」 질의에 걸린 소방청장 임명 기사 | `IRRELEVANT` | 동일 |
| 「염소 가스 누출」 질의에 걸린 수돗물 공급 홍보 기사 | `IRRELEVANT` | 염소는 소독 공정 언급일 뿐 |
| 「화학무기 사용」 질의에 걸린 핵무기 위협 기사 | `IRRELEVANT` | 대상 수단이 다르다 |

**수단 불일치 원칙**: 질의가 특정 수단(화학무기·폭발물·드론)을 지정했는데 기사가 다른 수단
(핵무기·총기·미사일)을 다루면 `IRRELEVANT`다. 단 기사가 두 수단을 함께 다루면 관련으로 본다.

## v2-2 INST 범위 확정
`INST`에 **포함**한다:
- 훈련·모의훈련·합동훈련·을지연습
- 점검·감시·검사·방재 활동
- 법령 발의·개정, 규제 강화·완화 논의
- 예산·조직·협약(MOU)·기술개발 사업
- **정부·공공기관이 발표하는 공식 사고 통계·현황** ← v1에서 누락돼 A·B가 갈렸던 지점

`INST`에서 **제외**한다:
- 민간 기업의 제품·기술 상용화 → `OTHER`
- 사업자의 설비 수명연장·해체 등 사업 절차와 그에 대한 반발 → `OTHER`

## v2-3 OTHER 정의 축소
`OTHER`는 **질의 대상과 관련이 있으나(v2-1 필요성 기준 충족) 사건도 제도 대응도 아닌 것**으로만 쓴다.
해설·기획·서평·산업동향·시장·인물·정치 공방이 여기 속한다.
판정이 애매하면 IRRELEVANT가 아니라 OTHER로 보낸다 — 성분 분해에서 OTHER는 보수적 잔여 범주다.

## v2-4 EVENT의 시간 경계 (B 라벨러 지적 반영)
사건 발생 후의 **수사·감정·판결·사과·후속 조치** 보도는 다음으로 가른다.

- 그 사건의 **사실관계를 갱신**하는 보도(입건·감정 결과·사망자 수 확정·사과) → `EVENT`
  (클러스터링이 최초 보도가 아님을 감지해 자동으로 `FOLLOW`로 분해한다)
- 사건이 **종결된 뒤 회고·기획·재조명**하는 보도(1주기, 「N년 뒤 가보니」, 연말기획) → `REF`

기준선: **사건 처리 절차가 진행 중이면 EVENT, 절차가 끝나고 의미를 되짚으면 REF.**

## v2-5 본 라벨링 절차
1. A·B 각각 코드북 v2로 2,867건 독립 라벨링
2. κ 재산출. **GATE: κ ≥ 0.70 미달 시 코드북 v3로 재조정 후 재실행**
3. 불일치 건은 조정(adjudication) 대상으로 분리. 조정 전후 성분 비율을 **모두** 보고한다
4. 인간 400건과 대조해 A·B의 체계적 편향 여부 확인
